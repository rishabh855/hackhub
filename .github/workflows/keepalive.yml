name: Keep Supabase Alive

on:
  schedule:
    - cron: '*/30 * * * *'  # every 30 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: curl -f https://hackhub-pi.vercel.app/api/keepalive || echo "Could not ping keepalive endpoint."
